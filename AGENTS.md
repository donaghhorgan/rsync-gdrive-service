# AGENTS.md

## Development workflow

- Use `git` for local git operations.
- Use `gh` for remote GitHub operations.
- Create a feature branch when working on new code. Open a PR for the changes.
- After making a code change, update [README.md](./README.md) and any other relevant documentation.
- Keep documentation concise.
